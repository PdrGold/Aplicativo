name: nome_do_projeto
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  image_picker: ^0.8.7+5
  video_player: ^2.5.0  # <-- pacote adicionado aqui
  audioplayers: ^5.2.1  # <-- pacote para tocar música

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/videos/    # pasta dos vídeos
    - assets/audio/     # pasta dos áudios


